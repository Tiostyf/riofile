# Example .env for backend (DO NOT commit real secrets)

# MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.ix3ubly.mongodb.net/<dbname>?retryWrites=true&w=majority&appName=Cluster0

# JWT secret (use a long random value in production)
JWT_SECRET=your_jwt_secret_here

# Server port
PORT=5001

# Frontend URL (for CORS)
CLIENT_URL=http://localhost:5173

# Node environment
NODE_ENV=development
